#
# Import the CMS Site configuration for the Travel store
#

# Language
$lang = en

# Create CMS Site
UPDATE CMSSite; uid[unique = true]; name[lang = $lang]; locale[lang = $lang]
; travel ; "Travel Site" ; en_GB
