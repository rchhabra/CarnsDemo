#
# Import the CMS Site configuration for the Travel store
#

# Language
$lang = en

# Create CMS Site
INSERT_UPDATE CMSSite; uid[unique = true]; name[lang = $lang]; locale[lang = $lang]
; airline ; "Cruises" ; en_GB
